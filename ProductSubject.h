//#pragma once
//#include"Subject.h"
//
//
//class ProductSubject :public Subject<ProductSubject> {
//public:
//	virtual void UseMoney(int _money);
//	virtual void TakeResrc(int _takeAmount);
//	virtual void Attach(IObserver* observer);
//	virtual void Detach(IObserver* observer);
//	virtual void Notify(IObserver* _observer);
//};